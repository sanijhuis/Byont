{
  "$schema": "https://turbo.build/schema.json",
  "env": {
    "JWT_SECRET": ""
  },
  "globalDependencies": ["/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/", ".next/", "!.next/cache/"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}